# 故障排除指南

如果项目无法访问，请按照以下步骤检查：

## 1. 检查环境

### 检查Python
```bash
python --version
```
应该显示 Python 3.8 或更高版本

### 检查Node.js
```bash
node --version
```
应该显示 Node.js 16 或更高版本

## 2. 运行诊断脚本

双击运行 `check_status.bat`，查看项目状态

## 3. 常见问题

### 问题1: 端口被占用

**症状**: 启动时提示端口已被占用

**解决方法**:
1. 检查端口占用：
   ```bash
   netstat -ano | findstr ":8000"  # 后端端口
   netstat -ano | findstr ":5173"  # 前端端口
   ```
2. 关闭占用端口的程序，或修改端口配置

### 问题2: 依赖未安装

**症状**: 启动时提示模块不存在

**解决方法**:
1. 后端：进入 `backend` 目录，运行：
   ```bash
   python -m venv venv
   venv\Scripts\activate
   pip install -r requirements.txt
   ```

2. 前端：进入 `frontend` 目录，运行：
   ```bash
   npm install
   ```

### 问题3: 无法访问前端

**症状**: 浏览器无法打开 http://localhost:5173

**检查清单**:
- [ ] 前端服务是否已启动？（查看前端服务窗口）
- [ ] 端口5173是否被占用？
- [ ] 防火墙是否阻止了访问？

### 问题4: API请求失败

**症状**: 前端可以打开，但上传文件或操作失败

**检查清单**:
- [ ] 后端服务是否已启动？（查看后端服务窗口）
- [ ] 后端是否在 http://localhost:8000 运行？
- [ ] 浏览器控制台是否有CORS错误？

### 问题5: 文件上传失败

**症状**: 上传文件时提示错误

**可能原因**:
1. 文件格式不支持（仅支持 .pptx, .ppt, .xlsx, .xls）
2. 文件过大
3. 后端服务未启动
4. 上传目录权限问题

## 4. 手动启动步骤

如果自动启动脚本不工作，可以手动启动：

### 启动后端

1. 打开命令行
2. 进入项目目录的 `backend` 文件夹
3. 运行：
   ```bash
   python -m venv venv
   venv\Scripts\activate
   pip install -r requirements.txt
   uvicorn main:app --reload --host 0.0.0.0 --port 8000
   ```

### 启动前端

1. 打开新的命令行窗口
2. 进入项目目录的 `frontend` 文件夹
3. 运行：
   ```bash
   npm install
   npm run dev
   ```

## 5. 验证服务运行

### 检查后端
在浏览器中访问：http://localhost:8000

应该看到：
```json
{"message": "文档生成工具 API"}
```

### 检查前端
在浏览器中访问：http://localhost:5173

应该看到文档生成工具的界面

## 6. 查看日志

### 后端日志
查看后端服务窗口，检查是否有错误信息

### 前端日志
查看前端服务窗口，检查是否有错误信息

### 浏览器控制台
按 F12 打开开发者工具，查看 Console 标签页的错误信息

## 7. 重置项目

如果以上方法都不行，可以尝试重置：

1. 删除 `backend/venv` 文件夹
2. 删除 `backend/uploads` 和 `backend/outputs` 文件夹（保留.gitkeep）
3. 删除 `frontend/node_modules` 文件夹
4. 重新运行启动脚本

## 8. 获取帮助

如果问题仍然存在，请提供以下信息：
- 操作系统版本
- Python版本
- Node.js版本
- 错误信息截图
- 后端和前端服务窗口的日志

