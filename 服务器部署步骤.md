# 服务器部署步骤指南

## 📋 准备工作

### 需要的信息：
- **服务器IP地址**：例如 `123.456.789.012`
- **服务器root密码**
- **操作系统**：Ubuntu 22.04（推荐）

---

## 🚀 部署步骤（3步完成）

### 步骤1：准备部署文件（Windows本地）

1. **双击运行**：`准备部署文件.bat`

2. **等待完成**（约2-5分钟）：
   - 自动检查项目
   - 安装前端依赖
   - 构建前端
   - 创建部署包

3. **结果**：
   - 生成 `deploy_package` 文件夹
   - 包含所有需要上传的文件

---

### 步骤2：连接服务器并上传文件

#### 方法A：使用FileZilla（推荐，最简单）

**2.1 下载FileZilla**
- 访问：https://filezilla-project.org/
- 下载并安装

**2.2 连接服务器**
1. 打开FileZilla
2. 点击 **文件** → **站点管理器** → **新站点**
3. 配置：
   - **协议**：选择 `SFTP - SSH File Transfer Protocol`
   - **主机**：输入你的服务器IP（例如：`123.456.789.012`）
   - **端口**：`22`
   - **登录类型**：选择 `正常`
   - **用户**：`root`
   - **密码**：输入你的root密码
4. 点击 **连接**

**2.3 上传文件**
1. **左侧窗口**（本地）：
   - 找到项目目录
   - 进入 `deploy_package` 文件夹
   - 全选所有文件和文件夹（Ctrl+A）

2. **右侧窗口**（服务器）：
   - 进入 `/var/www` 目录
   - 如果没有该目录，可以在根目录右键 → 创建目录 → 输入 `var/www`

3. **开始上传**：
   - 将左侧的文件拖到右侧的 `/var/www` 目录
   - 或右键左侧文件 → **上传**
   - 等待上传完成（可能需要几分钟）

4. **重命名文件夹**（在服务器SSH中操作，见下面方法B）

---

#### 方法B：使用SSH和SCP（命令行方式）

**2.1 连接服务器（SSH）**

**Windows Terminal / PowerShell：**
```powershell
ssh root@你的服务器IP
```
例如：
```powershell
ssh root@123.456.789.012
```

**首次连接：**
- 输入 `yes` 确认
- 输入root密码（输入时不会显示，直接输入后按Enter）

**2.2 上传文件（SCP）**

在**新的**PowerShell窗口（保持SSH连接打开），运行：

```powershell
scp -r deploy_package root@你的服务器IP:/var/www/document-tool
```

例如：
```powershell
scp -r deploy_package root@123.456.789.012:/var/www/document-tool
```

**如果 `/var/www` 不存在，先创建：**
在SSH连接中运行：
```bash
mkdir -p /var/www
```

---

### 步骤3：在服务器上运行部署脚本

**3.1 连接到服务器（如果还没连接）**
```bash
ssh root@你的服务器IP
```

**3.2 进入项目目录**
```bash
cd /var/www/document-tool
```

（如果使用方法A上传，需要先重命名：`cd /var/www` 然后 `mv deploy_package document-tool`）

**3.3 给脚本添加执行权限**
```bash
chmod +x 一键部署脚本.sh
```

**3.4 运行部署脚本**
```bash
sudo bash 一键部署脚本.sh
```

**3.5 等待部署完成**（约10-15分钟）

脚本会自动：
- ✅ 安装Python、Node.js、Nginx等环境
- ✅ 构建前端
- ✅ 配置后端
- ✅ 启动服务
- ✅ 配置防火墙

**3.6 输入域名（可选）**
- 如果有域名，输入域名
- 如果没有域名，直接**回车**使用IP访问

---

### 步骤4：配置云服务器安全组（重要！）

**如果不配置安全组，外部无法访问你的网站！**

#### 阿里云
1. 登录阿里云控制台
2. 进入 **云服务器ECS** → **实例**
3. 点击你的服务器
4. 点击 **安全组** → **配置规则**
5. **添加规则**：
   - **端口范围**：`80/80`
   - **授权对象**：`0.0.0.0/0`
   - **协议类型**：`TCP`
   - **描述**：HTTP
6. **再次添加**：端口 `443`（HTTPS，可选）

#### 腾讯云
1. 登录腾讯云控制台
2. 进入 **云服务器** → **实例**
3. 点击服务器 → **安全组**
4. 点击 **修改规则** → **添加规则**
5. **配置**：
   - **类型**：`自定义`
   - **协议端口**：`TCP:80`
   - **来源**：`0.0.0.0/0`
6. **再次添加**：`TCP:443`

#### 华为云
1. 登录华为云控制台
2. 进入 **弹性云服务器** → **安全组**
3. 选择安全组 → **入方向规则** → **添加规则**
4. **配置**：
   - **协议端口**：`TCP:80`
   - **源地址**：`0.0.0.0/0`
5. **再次添加**：`TCP:443`

---

### 步骤5：测试访问

1. **浏览器访问**：
   ```
   http://你的服务器IP
   ```
   例如：`http://123.456.789.012`

2. **应该看到**：
   - 文档生成工具的前端界面
   - 可以上传文件
   - 可以操作

3. **测试API**：
   ```
   http://你的服务器IP/api/
   ```
   应该返回：`{"message":"文档生成工具 API"}`

---

## ✅ 部署成功标志

- ✓ 可以通过 `http://你的服务器IP` 访问前端
- ✓ 可以通过 `http://你的服务器IP/api/` 访问后端
- ✓ 可以上传文件
- ✓ 可以下载生成的文件

---

## 🔧 常用命令（服务器上）

### 查看服务状态
```bash
# 查看后端服务
pm2 status

# 查看Nginx状态
sudo systemctl status nginx
```

### 查看日志
```bash
# 后端日志
pm2 logs document-backend

# Nginx错误日志
sudo tail -f /var/log/nginx/error.log
```

### 重启服务
```bash
# 重启后端
pm2 restart document-backend

# 重启Nginx
sudo systemctl restart nginx
```

---

## ❓ 遇到问题？

### 问题1：无法连接服务器
- 检查服务器IP是否正确
- 检查服务器是否运行
- 检查安全组是否开放22端口（SSH）

### 问题2：上传失败
- 检查网络连接
- 检查服务器磁盘空间：`df -h`
- 使用SCP方法重试

### 问题3：部署脚本失败
- 查看错误信息
- 检查网络连接（需要下载依赖）
- 检查服务器磁盘空间

### 问题4：无法访问网站
- **检查安全组**：是否开放80端口（最重要！）
- 检查Nginx：`sudo systemctl status nginx`
- 检查后端：`pm2 status`
- 查看日志：`pm2 logs document-backend`

### 问题5：需要重新部署
```bash
cd /var/www/document-tool
sudo bash 一键部署脚本.sh
```

---

## 📞 需要更多帮助？

- 查看 **`完整部署指南.md`** - 详细说明
- 查看 **`部署检查清单.md`** - 检查清单
- 查看 **`故障排除.md`** - 故障排除指南

---

**祝你部署顺利！** 🎉

