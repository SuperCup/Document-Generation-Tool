# 打包说明

## 将项目打包成 Windows exe 文件

### 前置要求

1. **Python 3.8+** - 已安装并添加到PATH
2. **Node.js 16+** - 已安装并添加到PATH
3. **npm** - 通常随Node.js一起安装

### 打包步骤

#### 方法一：使用自动打包脚本（推荐）

1. 打开命令行，进入项目根目录
2. 运行打包脚本：
   ```bash
   python build.py
   ```

脚本会自动完成：
- 清理旧的构建文件
- 构建前端静态文件
- 检查并安装依赖
- 打包成exe文件

#### 方法二：手动打包

1. **构建前端**
   ```bash
   cd frontend
   npm install
   npm run build
   cd ..
   ```
   构建完成后，将 `frontend/dist` 目录重命名为 `frontend_dist` 并移动到项目根目录

2. **安装PyInstaller**
   ```bash
   pip install pyinstaller
   ```

3. **打包exe**
   ```bash
   cd backend
   pyinstaller --clean --noconfirm build_exe.spec
   cd ..
   ```

### 打包结果

打包完成后，会在 `dist` 目录下生成：
- `DocumentGenerationTool.exe` - 主程序
- `backend/` - 后端代码和数据
- `frontend_dist/` - 前端静态文件

### 分发应用

将整个 `dist` 目录打包分发给用户。用户只需要：
1. 解压文件
2. 双击 `DocumentGenerationTool.exe` 运行

### 注意事项

1. **首次运行**：exe文件首次运行可能会被Windows Defender拦截，需要点击"更多信息" -> "仍要运行"

2. **杀毒软件**：某些杀毒软件可能会误报，这是PyInstaller打包的常见问题，可以添加白名单

3. **文件大小**：打包后的exe文件可能较大（100MB+），这是正常的，因为包含了Python运行时和所有依赖

4. **端口占用**：确保8000和5173端口未被占用

5. **防火墙**：首次运行时，Windows防火墙可能会询问是否允许访问，请选择"允许"

### 故障排除

#### 问题1：打包失败 - 找不到模块
**解决方案**：确保所有依赖都已安装
```bash
pip install -r backend/requirements.txt
```

#### 问题2：exe运行后无法访问
**解决方案**：
- 检查防火墙设置
- 查看控制台输出的错误信息
- 确保端口未被占用

#### 问题3：前端页面无法加载
**解决方案**：
- 确保 `frontend_dist` 目录存在且包含index.html
- 检查构建是否成功

#### 问题4：文件上传失败
**解决方案**：
- 确保 `backend/uploads` 和 `backend/outputs` 目录存在
- 检查文件权限

### 优化建议

1. **减小文件大小**：
   - 使用 `--onefile` 模式（已在spec文件中配置）
   - 排除不必要的模块

2. **添加图标**：
   - 准备一个 `.ico` 文件
   - 在 `build_exe.spec` 中修改 `icon=None` 为 `icon='path/to/icon.ico'`

3. **版本信息**：
   - 创建版本信息文件 `version_info.txt`
   - 在spec文件中添加版本信息

### 开发模式 vs 打包模式

- **开发模式**：使用 `快速启动.bat` 或分别启动前后端
- **打包模式**：运行exe文件，自动启动所有服务

两种模式功能完全相同，只是启动方式不同。

