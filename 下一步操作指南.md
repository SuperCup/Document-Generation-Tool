# 下一步操作指南

根据你的ECS实例信息，以下是详细的操作步骤。

## 📋 你的实例信息

- **实例ID**: i-7xvg439r0716r4k66ut8
- **公网IP**: 8.134.59.82
- **系统**: Ubuntu 22.04 64位
- **状态**: 运行中
- **配置**: 2核4GiB

---

## 🚀 立即执行的5个步骤

### 步骤1：配置安全组（必须！）

**操作位置：** 阿里云ECS控制台

1. 在实例列表，点击你的实例ID：`i-7xvg439r0716r4k66ut8`
2. 点击 **网络与安全组** 标签页
3. 找到 **安全组**，点击安全组ID（蓝色链接）
4. 点击 **配置规则** 按钮
5. 点击 **添加安全组规则**

**添加以下3条规则：**

**规则1：SSH访问**
```
规则方向：入方向
授权策略：允许
协议类型：TCP
端口范围：22/22
授权对象：0.0.0.0/0
描述：SSH连接
```

**规则2：HTTP访问**
```
规则方向：入方向
授权策略：允许
协议类型：TCP
端口范围：80/80
授权对象：0.0.0.0/0
描述：HTTP访问
```

**规则3：HTTPS访问**
```
规则方向：入方向
授权策略：允许
协议类型：TCP
端口范围：443/443
授权对象：0.0.0.0/0
描述：HTTPS访问
```

**完成后点击确定**

---

### 步骤2：重置root密码

**操作位置：** 阿里云ECS控制台

1. 在实例页面，点击 **重置密码** 按钮
2. 输入新密码（建议使用强密码）
3. 勾选 **重启实例**
4. 点击 **确定**

**⚠️ 重要：**
- 密码必须包含：大小写字母、数字、特殊字符
- 至少8位
- **请务必记录密码！**

**等待实例重启完成（约2-3分钟）**

---

### 步骤3：连接服务器

**在本地电脑操作：**

#### Windows系统

**方法1：使用Windows Terminal（推荐）**

1. 打开Windows Terminal或PowerShell
2. 运行命令：
   ```bash
   ssh root@8.134.59.82
   ```
3. 首次连接会提示，输入 `yes`
4. 输入刚才设置的root密码
5. 连接成功！

**方法2：使用PuTTY**

1. 打开PuTTY
2. Host Name: `8.134.59.82`
3. Port: `22`
4. Connection type: `SSH`
5. 点击Open
6. 输入用户名：`root`
7. 输入密码

#### Mac/Linux系统

直接在终端运行：
```bash
ssh root@8.134.59.82
```

**连接成功后，你会看到类似：**
```
Welcome to Ubuntu 22.04.3 LTS
root@iZ7xvg439r0716r4k66ut8Z:~#
```

---

### 步骤4：上传项目文件

**选择一种方法：**

#### 方法1：使用Git（如果项目在Git仓库）

```bash
# 在服务器上执行
cd /var/www
git clone <your-repo-url> document-tool
```

#### 方法2：使用FTP工具（推荐，简单）

**使用FileZilla：**

1. 下载FileZilla：https://filezilla-project.org/
2. 打开FileZilla
3. 文件 -> 站点管理器 -> 新建站点
4. 配置：
   - 协议：SFTP
   - 主机：`8.134.59.82`
   - 端口：`22`
   - 用户名：`root`
   - 密码：你设置的密码
5. 连接
6. 左侧：选择本地项目文件夹（`D:\Project\Document-Generation-Tool`）
7. 右侧：进入 `/var/www`，创建 `document-tool` 文件夹
8. 上传整个项目文件夹

**上传时间：** 约5-10分钟（取决于文件大小）

#### 方法3：使用scp命令（在本地项目目录）

```bash
# 在本地电脑的PowerShell或终端运行
# 先进入项目目录
cd D:\Project\Document-Generation-Tool

# 上传文件
scp -r . root@8.134.59.82:/var/www/document-tool/
```

---

### 步骤5：运行部署脚本

**在服务器SSH连接中执行：**

```bash
# 进入项目目录
cd /var/www/document-tool

# 给脚本添加执行权限
chmod +x 一键部署脚本.sh

# 运行部署脚本
sudo bash 一键部署脚本.sh
```

**脚本会自动：**
1. 安装Python、Node.js、Nginx、PM2
2. 构建前端
3. 配置后端
4. 配置Nginx
5. 启动服务

**预计时间：** 15-20分钟

**脚本运行过程中：**
- 会询问域名（直接回车使用IP）
- 会自动安装所有依赖
- 会显示进度信息

**完成后会显示：**
```
🎉 部署完成！
访问地址: http://8.134.59.82
```

---

## ✅ 验证部署

### 1. 测试后端API

在浏览器访问：
```
http://8.134.59.82/api/
```

**应该看到：**
```json
{"message":"文档生成工具 API"}
```

### 2. 测试前端

在浏览器访问：
```
http://8.134.59.82/
```

**应该看到应用界面**

### 3. 测试功能

1. 上传一个PPT或Excel文件
2. 选择图片
3. 插入页面/图片
4. 下载生成的文件

---

## 🔍 如果遇到问题

### 问题1：无法SSH连接

**检查：**
1. 安全组是否开放了22端口
2. 密码是否正确
3. 实例是否已重启完成

### 问题2：无法访问网站

**检查：**
1. 安全组是否开放了80端口
2. Nginx是否运行：`sudo systemctl status nginx`
3. 后端是否运行：`pm2 status`

### 问题3：部署脚本失败

**查看详细错误：**
```bash
# 查看脚本输出
# 检查具体哪一步失败

# 手动执行失败的步骤
# 参考 云服务器部署详细指南.md
```

---

## 📊 监控费用

### 设置余额提醒

1. 登录阿里云控制台
2. 进入 **费用中心** -> **费用预警**
3. 设置：
   - 余额低于 50元：发送通知
   - 余额低于 10元：发送通知

### 查看实时费用

1. **费用中心** -> **费用账单** -> **实时账单**
2. 关注：
   - ECS实例费用
   - 公网流量费用（CDT）

### 查看流量使用

1. 在ECS控制台，点击你的实例
2. 点击 **监控** 标签
3. 查看 **网络流量** 图表

---

## 🎯 快速命令参考

```bash
# 连接服务器
ssh root@8.134.59.82

# 查看服务状态
pm2 status
sudo systemctl status nginx

# 查看日志
pm2 logs document-backend
sudo tail -f /var/log/nginx/error.log

# 重启服务
pm2 restart document-backend
sudo systemctl restart nginx

# 查看系统资源
htop
df -h  # 查看磁盘使用
```

---

## 📝 操作检查清单

按顺序完成：

- [ ] **步骤1**：配置安全组（开放22, 80, 443端口）
- [ ] **步骤2**：重置root密码并重启实例
- [ ] **步骤3**：SSH连接服务器成功
- [ ] **步骤4**：项目文件已上传到服务器
- [ ] **步骤5**：运行部署脚本成功
- [ ] **验证**：可以通过IP访问网站
- [ ] **安全**：设置余额提醒

---

## 💡 提示

1. **保存密码**：将root密码保存在安全的地方
2. **记录IP**：公网IP `8.134.59.82` 是访问地址
3. **定期检查**：每天查看一次费用和流量使用
4. **及时备份**：重要数据及时备份

按照以上步骤操作，你的项目就可以在云服务器上运行了！

如有问题，请查看 `云服务器部署详细指南.md` 获取更详细的帮助。

