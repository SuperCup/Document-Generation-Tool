# 文件自动清理功能说明

## 功能概述

为了节省服务器存储空间，系统现在会在以下情况自动删除临时文件：
1. **图片文件**：图片插入到PPT或Excel文件后，自动删除临时上传的图片文件
2. **导出文件**：用户下载生成的文件后，自动删除服务器上的导出文件
3. **原始上传文件**：用户下载生成的文件后，自动删除服务器上的原始上传文件（PPT/Excel）

## 工作原理

1. **图片上传**：
   - 用户上传图片时，图片临时保存在 `backend/uploads/` 目录
   - 文件名格式：`images_{uuid}.{ext}` 或 `remote_{uuid}.{ext}`

2. **图片插入**：
   - 图片被插入到PPT或Excel文件中
   - 图片内容已经嵌入到文档中

3. **自动清理**：
   - 插入操作成功后，系统自动删除临时图片文件
   - 只删除已使用的图片，不会影响其他文件

## 安全特性

- ✅ **路径安全检查**：只删除 `uploads` 目录下的图片文件
- ✅ **文件类型验证**：只删除临时图片文件（`images_` 或 `remote_` 开头，或图片扩展名）
- ✅ **错误处理**：删除失败不会影响主流程，只记录日志
- ✅ **防止误删**：不会删除其他重要文件

## 影响的接口

### 图片自动删除

以下接口在插入成功后会自动删除图片：

1. **PPT插入页面** (`/api/ppt/insert-page`)
   - 插入页面后删除所有使用的图片

2. **Excel单张图片插入** (`/api/excel/insert-image`)
   - 插入单张图片后删除该图片

3. **Excel批量图片插入** (`/api/excel/insert-images-batch`)
   - 批量插入后删除所有使用的图片

### 导出文件和原始文件自动删除

以下接口在下载完成后会自动删除相关文件：

1. **下载生成的文件** (`/api/download/{file_id}`)
   - 文件下载完成后，自动删除服务器上的导出文件（`*_modified.*`）
   - 同时自动删除原始上传文件（`{file_id}.pptx`, `{file_id}.xlsx` 等）
   - 使用后台任务确保文件传输完成后再删除

## 日志记录

删除操作会记录在日志中：
- 成功删除：`INFO: 已删除临时图片: {path}`
- 删除失败：`ERROR: 删除图片文件失败 {path}: {error}`

## 注意事项

1. **已插入的图片**：图片内容已经嵌入到PPT/Excel文件中，删除临时文件不影响文档
2. **未使用的图片**：如果图片上传后未使用，不会自动删除（需要手动清理或定期清理）
3. **远程图片**：从远程服务下载的图片也会在插入后删除
4. **导出文件**：文件下载完成后才会删除，确保用户能够成功下载
5. **原始上传文件**：在用户下载导出文件后自动删除，因为已经生成了修改后的版本
6. **文件已下载**：删除服务器上的文件不影响用户已下载的本地文件
7. **再次操作**：如果用户需要再次操作同一个文件，需要重新上传（因为原始文件已删除）

## 手动清理（可选）

如果需要清理未使用的文件，可以：

```bash
# 清理所有临时图片（谨慎使用）
find backend/uploads -name "images_*" -type f -delete
find backend/uploads -name "remote_*" -type f -delete

# 清理所有导出文件（谨慎使用）
find backend/outputs -name "*_modified.*" -type f -delete
```

或者添加定期清理任务（cron job）来清理超过一定时间的未使用文件。

## 存储空间优化

通过自动清理功能：
- ✅ 图片插入后立即删除，不占用存储空间
- ✅ 文件下载后立即删除导出文件和原始上传文件，不占用存储空间
- ✅ 减少服务器存储压力
- ✅ 提高系统运行效率
- ✅ 所有临时文件在使用后自动清理，无需手动维护

## 文件生命周期

1. **上传阶段**：
   - 用户上传PPT/Excel文件 → 保存在 `uploads/` 目录
   - 用户上传图片 → 保存在 `uploads/` 目录

2. **处理阶段**：
   - 图片插入到文档 → 图片内容嵌入文档，临时图片文件删除
   - 生成修改后的文档 → 保存在 `outputs/` 目录

3. **下载阶段**：
   - 用户下载修改后的文档 → 文件传输给用户
   - 下载完成后 → 自动删除导出文件和原始上传文件

**结果**：所有文件在使用后都会被自动清理，服务器不保留任何临时文件。

