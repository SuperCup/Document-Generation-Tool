# 快速部署指南

## 🚀 方案一：内网穿透（5分钟，最快）

### 使用ngrok（推荐）

#### 1. 安装ngrok
- 访问：https://ngrok.com/download
- 下载Windows版本
- 解压到任意目录（如 `C:\ngrok`）

#### 2. 注册并配置
```bash
# 注册账号后获取authtoken
ngrok config add-authtoken YOUR_TOKEN
```

#### 3. 一键部署
双击运行：`部署_内网穿透.bat`

#### 4. 启动ngrok
打开新命令行，运行：
```bash
ngrok http 5173
```

#### 5. 访问
ngrok会显示类似 `https://xxxx.ngrok.io` 的地址，通过此地址访问。

**优点**：5分钟搞定，无需服务器  
**缺点**：免费版URL每次变化，有流量限制

---

## 🖥️ 方案二：云服务器部署（30分钟，正式）

### 前置准备
- 云服务器（阿里云/腾讯云/华为云）
- 域名（可选，可直接用IP）

### 步骤1：连接服务器
```bash
ssh root@your-server-ip
```

### 步骤2：运行部署脚本
```bash
# 上传部署脚本到服务器
chmod +x 部署_云服务器.sh
sudo ./部署_云服务器.sh
```

### 步骤3：上传项目文件
使用FTP/SFTP或Git将项目上传到 `/var/www/document-tool`

### 步骤4：构建和启动
```bash
cd /var/www/document-tool

# 构建前端
cd frontend
npm install
npm run build
cd ..

# 配置后端
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 使用PM2启动后端
pm2 start "uvicorn main:app --host 0.0.0.0 --port 8000" --name backend
pm2 save
pm2 startup
```

### 步骤5：配置Nginx
编辑 `/etc/nginx/sites-available/document-tool`，修改 `server_name` 为你的域名或IP：
```nginx
server_name your-domain.com;  # 或 your-server-ip
```

重启Nginx：
```bash
sudo nginx -t
sudo systemctl reload nginx
```

### 步骤6：访问
通过 `http://your-domain.com` 或 `http://your-server-ip` 访问

---

## 🐳 方案三：Docker部署（推荐生产环境）

### 前提条件
- 已安装Docker和Docker Compose

### 一键部署
```bash
# 构建并启动
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止
docker-compose down
```

### 访问
- 前端：http://localhost:8080
- 后端API：http://localhost:8000

---

## 📋 详细配置说明

### CORS配置
后端已支持通过环境变量配置CORS：
```bash
# 开发环境（允许所有）
# 不设置环境变量，默认允许所有来源

# 生产环境（限制特定域名）
export ALLOWED_ORIGINS=http://your-domain.com,https://your-domain.com
```

### 防火墙配置
```bash
# Ubuntu
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 8000/tcp

# Windows
# 在防火墙设置中添加入站规则
```

### SSL证书配置（HTTPS）
```bash
# 安装certbot
sudo apt install certbot python3-certbot-nginx

# 获取证书
sudo certbot --nginx -d your-domain.com

# 自动续期
sudo certbot renew --dry-run
```

---

## 🔧 常见问题

### Q1: 外部无法访问？
**检查清单：**
1. ✅ 防火墙是否开放端口
2. ✅ 云服务器安全组是否配置
3. ✅ 服务是否绑定到 `0.0.0.0`（不是 `127.0.0.1`）
4. ✅ CORS配置是否正确

### Q2: 上传大文件失败？
**解决方案：**
1. Nginx配置：`client_max_body_size 100M;`
2. 检查服务器磁盘空间
3. 检查网络连接

### Q3: 服务重启后停止？
**解决方案：**
使用PM2管理进程：
```bash
pm2 save
pm2 startup
```

### Q4: 如何更新代码？
```bash
# 拉取最新代码
git pull

# 重新构建前端
cd frontend && npm run build

# 重启后端
pm2 restart backend

# 或Docker方式
docker-compose restart
```

---

## 📊 性能优化建议

1. **启用Gzip压缩**（Nginx已配置）
2. **使用CDN加速静态资源**
3. **配置Redis缓存**（如需要）
4. **使用负载均衡**（高并发场景）

---

## 🔒 安全建议

1. **必须使用HTTPS**（生产环境）
2. **限制CORS来源**（不要使用 `*`）
3. **配置防火墙规则**
4. **定期更新依赖包**
5. **设置访问日志和监控**

---

## 📞 需要帮助？

如果遇到问题，请检查：
1. 服务器日志：`pm2 logs backend`
2. Nginx日志：`/var/log/nginx/error.log`
3. 防火墙状态：`sudo ufw status`

