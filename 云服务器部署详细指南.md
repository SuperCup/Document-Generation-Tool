# 云服务器部署详细指南

本指南将详细说明如何在云服务器上部署文档生成工具，每一步都有具体的命令和说明。

---

## 📋 前置准备

### 1. 购买云服务器

**推荐配置：**
- **CPU**: 2核（免费试用可选4核）
- **内存**: 4GB（免费试用可选8GB）
- **带宽**: 5Mbps
- **系统**: Ubuntu 22.04 LTS（推荐）或 Ubuntu 20.04 LTS
- **磁盘**: 40GB SSD

**推荐平台：**
- 阿里云：https://www.aliyun.com（有免费试用）
- 腾讯云：https://cloud.tencent.com
- 华为云：https://www.huaweicloud.com

**⚠️ 重要：预装应用选择**
- **不要选择任何预装应用**（不选宝塔、WordPress、Docker、LNMP）
- 原因：我们的部署脚本会自动安装所需环境，预装应用可能产生冲突
- 只需要选择操作系统：**Ubuntu 22.04 64位**

### 2. 获取服务器信息

购买后，你会获得：
- **公网IP地址**：例如 `123.456.789.012`（你的IP：8.134.59.82）
- **root密码**：或SSH密钥（需要重置）
- **SSH端口**：通常是 `22`

**⚠️ 免费试用注意事项：**
- 查看 `免费试用注意事项.md` 了解如何避免扣费
- 设置余额提醒和自动释放
- 监控流量使用情况

### 3. 准备本地工具

- **SSH客户端**：
  - Windows: PuTTY 或 Windows Terminal
  - Mac/Linux: 自带终端
- **FTP工具**（可选）：
  - FileZilla
  - WinSCP

---

## 🔐 第一步：连接服务器

### Windows系统

#### 方法1：使用Windows Terminal（推荐）

1. 打开Windows Terminal或PowerShell
2. 运行以下命令：
   ```bash
   ssh root@你的服务器IP
   ```
   例如：
   ```bash
   ssh root@123.456.789.012
   ```
3. 首次连接会提示确认，输入 `yes`
4. 输入root密码（输入时不会显示，直接输入后按Enter）

#### 方法2：使用PuTTY

1. 下载并打开PuTTY
2. 在Host Name输入：`root@你的服务器IP`
3. Port保持：`22`
4. Connection type选择：`SSH`
5. 点击Open
6. 输入root密码

### Mac/Linux系统

直接在终端运行：
```bash
ssh root@你的服务器IP
```

---

## 🛠️ 第二步：初始化服务器环境

### 2.1 更新系统

连接成功后，首先更新系统：

```bash
# 更新软件包列表
sudo apt update

# 升级已安装的软件包
sudo apt upgrade -y
```

**预期输出：**
```
Reading package lists... Done
Building dependency tree... Done
...
```

**等待时间：** 5-10分钟（取决于服务器性能）

### 2.2 安装基础工具

```bash
# 安装常用工具
sudo apt install -y curl wget git vim
```

**验证安装：**
```bash
# 检查版本
python3 --version  # 应该显示 Python 3.10.x 或更高
curl --version
git --version
```

---

## 🐍 第三步：安装Python环境

### 3.1 检查Python版本

```bash
python3 --version
```

**如果显示 Python 3.10+，可以跳过安装步骤**

### 3.2 安装Python（如果需要）

```bash
# 安装Python 3.10和pip
sudo apt install -y python3 python3-pip python3-venv

# 验证安装
python3 --version
pip3 --version
```

**预期输出：**
```
Python 3.10.12
pip 22.0.2 from /usr/lib/python3/dist-packages/pip (python 3.10)
```

### 3.3 配置pip使用国内镜像（可选，加速下载）

```bash
# 创建pip配置目录
mkdir -p ~/.pip

# 创建配置文件
cat > ~/.pip/pip.conf << 'EOF'
[global]
index-url = https://pypi.tuna.tsinghua.edu.cn/simple
[install]
trusted-host = pypi.tuna.tsinghua.edu.cn
EOF
```

---

## 📦 第四步：安装Node.js

### 4.1 安装Node.js 18.x

```bash
# 添加NodeSource仓库
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -

# 安装Node.js
sudo apt install -y nodejs
```

### 4.2 验证安装

```bash
node --version
npm --version
```

**预期输出：**
```
v18.17.0
9.6.7
```

### 4.3 配置npm使用国内镜像（可选，加速下载）

```bash
npm config set registry https://registry.npmmirror.com
```

---

## 🌐 第五步：安装Nginx

### 5.1 安装Nginx

```bash
sudo apt install -y nginx
```

### 5.2 启动Nginx

```bash
# 启动Nginx服务
sudo systemctl start nginx

# 设置开机自启
sudo systemctl enable nginx

# 检查状态
sudo systemctl status nginx
```

**预期输出：**
```
● nginx.service - A high performance web server and a reverse proxy server
     Loaded: loaded (/lib/systemd/system/nginx.service; enabled; vendor preset: enabled)
     Active: active (running) since ...
```

### 5.3 测试Nginx

在浏览器访问：`http://你的服务器IP`

应该看到Nginx欢迎页面。

---

## 🔧 第六步：安装PM2（进程管理）

### 6.1 安装PM2

```bash
sudo npm install -g pm2
```

### 6.2 验证安装

```bash
pm2 --version
```

**预期输出：**
```
5.3.0
```

### 6.3 配置PM2开机自启

```bash
# 生成启动脚本
pm2 startup

# 按照提示执行命令（会显示类似下面的命令）
# sudo env PATH=$PATH:/usr/bin /usr/lib/node_modules/pm2/bin/pm2 startup systemd -u root --hp /root
```

---

## 📁 第七步：上传项目文件

### 方法1：使用Git（推荐）

#### 7.1 在服务器上克隆项目

```bash
# 创建项目目录
sudo mkdir -p /var/www
cd /var/www

# 克隆项目（如果项目在Git仓库）
# git clone https://github.com/your-username/your-repo.git document-tool

# 或者创建目录
sudo mkdir -p document-tool
cd document-tool
```

#### 7.2 如果项目不在Git，使用FTP上传

**使用FileZilla上传：**

1. 打开FileZilla
2. 文件 -> 站点管理器
3. 新建站点：
   - 协议：SFTP
   - 主机：你的服务器IP
   - 端口：22
   - 用户名：root
   - 密码：你的root密码
4. 连接
5. 左侧：选择本地项目文件夹
6. 右侧：进入 `/var/www/document-tool`
7. 上传整个项目文件夹

**使用命令行上传（如果有本地SSH访问）：**

```bash
# 在本地项目目录运行
scp -r . root@你的服务器IP:/var/www/document-tool/
```

### 7.3 设置文件权限

```bash
cd /var/www/document-tool

# 设置目录所有者
sudo chown -R $USER:$USER .

# 设置权限
chmod -R 755 .
```

---

## 🏗️ 第八步：构建前端

### 8.1 进入前端目录

```bash
cd /var/www/document-tool/frontend
```

### 8.2 安装依赖

```bash
npm install
```

**预期输出：**
```
added 1234 packages in 2m
```

**等待时间：** 2-5分钟

### 8.3 构建前端

```bash
npm run build
```

**预期输出：**
```
vite v5.0.8 building for production...
✓ 1234 modules transformed.
dist/index.html                   1.23 kB
dist/assets/index-abc123.js       234.56 kB
...
```

**等待时间：** 1-2分钟

### 8.4 验证构建结果

```bash
ls -la dist/
```

应该看到 `index.html` 和 `assets` 目录。

---

## 🐍 第九步：配置后端

### 9.1 进入后端目录

```bash
cd /var/www/document-tool/backend
```

### 9.2 创建虚拟环境

```bash
python3 -m venv venv
```

### 9.3 激活虚拟环境

```bash
source venv/bin/activate
```

**提示符会变成：**
```
(venv) root@server:~/document-tool/backend#
```

### 9.4 安装依赖

```bash
pip install -r requirements.txt
```

**预期输出：**
```
Collecting fastapi==0.104.1
  Downloading fastapi-0.104.1-py3-none-any.whl (92 kB)
...
Successfully installed fastapi-0.104.1 ...
```

**等待时间：** 2-5分钟

### 9.5 创建必要的目录

```bash
mkdir -p uploads outputs
chmod 755 uploads outputs
```

### 9.6 测试后端启动

```bash
# 临时启动测试
uvicorn main:app --host 0.0.0.0 --port 8000
```

**预期输出：**
```
INFO:     Started server process [12345]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
```

**按 Ctrl+C 停止测试**

---

## ⚙️ 第十步：配置Nginx反向代理

### 10.1 创建Nginx配置文件

```bash
sudo nano /etc/nginx/sites-available/document-tool
```

### 10.2 输入以下配置

```nginx
server {
    listen 80;
    server_name _;  # 替换为你的域名，或保持_使用IP访问

    # 前端静态文件
    location / {
        root /var/www/document-tool/frontend/dist;
        try_files $uri $uri/ /index.html;
        index index.html;
    }

    # 后端API代理
    location /api {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 文件上传大小限制
        client_max_body_size 100M;
        
        # 超时设置
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        root /var/www/document-tool/frontend/dist;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

**保存文件：**
- 按 `Ctrl+O` 保存
- 按 `Enter` 确认
- 按 `Ctrl+X` 退出

### 10.3 启用配置

```bash
# 创建符号链接
sudo ln -s /etc/nginx/sites-available/document-tool /etc/nginx/sites-enabled/

# 删除默认配置（可选）
sudo rm /etc/nginx/sites-enabled/default
```

### 10.4 测试配置

```bash
sudo nginx -t
```

**预期输出：**
```
nginx: the configuration file /etc/nginx/nginx.conf syntax is ok
nginx: configuration file /etc/nginx/nginx.conf test is successful
```

### 10.5 重载Nginx

```bash
sudo systemctl reload nginx
```

---

## 🚀 第十一步：使用PM2启动后端

### 11.1 创建PM2启动脚本

```bash
cd /var/www/document-tool/backend
source venv/bin/activate

# 使用PM2启动
pm2 start "uvicorn main:app --host 0.0.0.0 --port 8000" --name document-backend
```

### 11.2 查看PM2状态

```bash
pm2 status
```

**预期输出：**
```
┌─────┬─────────────────────┬─────────┬─────────┬──────────┐
│ id  │ name                │ status  │ restart │ uptime   │
├─────┼─────────────────────┼─────────┼─────────┼──────────┤
│ 0   │ document-backend    │ online  │ 0       │ 0s       │
└─────┴─────────────────────┴─────────┴─────────┴──────────┘
```

### 11.3 查看日志

```bash
pm2 logs document-backend
```

### 11.4 保存PM2配置

```bash
pm2 save
```

这样服务器重启后会自动启动后端服务。

---

## 🔥 第十二步：配置防火墙

### 12.1 检查防火墙状态

```bash
sudo ufw status
```

### 12.2 配置防火墙规则

```bash
# 允许SSH（必须，否则会断开连接）
sudo ufw allow 22/tcp

# 允许HTTP
sudo ufw allow 80/tcp

# 允许HTTPS（如果配置了SSL）
sudo ufw allow 443/tcp

# 允许后端API端口（可选，因为已经通过Nginx代理）
sudo ufw allow 8000/tcp
```

### 12.3 启用防火墙

```bash
sudo ufw enable
```

**确认提示输入 `y`**

### 12.4 验证防火墙规则

```bash
sudo ufw status numbered
```

---

## 🌍 第十三步：配置云服务器安全组

### 阿里云

1. 登录阿里云控制台
2. 进入 **云服务器ECS** -> **实例**
3. 点击你的服务器实例
4. 点击 **安全组** -> **配置规则**
5. 添加规则：
   - **端口范围**: `80/80`
   - **授权对象**: `0.0.0.0/0`
   - **协议类型**: `TCP`
6. 同样添加 `443` 端口（HTTPS）

### 腾讯云

1. 登录腾讯云控制台
2. 进入 **云服务器** -> **实例**
3. 点击你的服务器 -> **安全组**
4. 点击 **修改规则**
5. 添加规则：
   - **类型**: `自定义`
   - **协议端口**: `TCP:80`
   - **来源**: `0.0.0.0/0`
6. 同样添加 `TCP:443`

### 华为云

1. 登录华为云控制台
2. 进入 **弹性云服务器** -> **安全组**
3. 选择你的安全组 -> **入方向规则** -> **添加规则**
4. 配置：
   - **协议端口**: `TCP:80`
   - **源地址**: `0.0.0.0/0`
5. 同样添加 `TCP:443`

---

## ✅ 第十四步：测试访问

### 14.1 测试后端API

在浏览器访问：
```
http://你的服务器IP/api/
```

**预期输出：**
```json
{"message":"文档生成工具 API"}
```

### 14.2 测试前端

在浏览器访问：
```
http://你的服务器IP/
```

应该看到应用界面。

### 14.3 测试文件上传

1. 访问前端页面
2. 尝试上传一个PPT或Excel文件
3. 检查是否能正常处理

---

## 🔒 第十五步：配置HTTPS（可选但推荐）

### 15.1 安装Certbot

```bash
sudo apt install -y certbot python3-certbot-nginx
```

### 15.2 获取SSL证书

```bash
# 如果有域名
sudo certbot --nginx -d your-domain.com

# 如果只有IP，可以使用自签名证书（不推荐用于生产）
```

### 15.3 自动续期

Certbot会自动配置自动续期，验证：
```bash
sudo certbot renew --dry-run
```

---

## 📊 第十六步：监控和维护

### 16.1 查看后端日志

```bash
# 实时日志
pm2 logs document-backend

# 最近100行
pm2 logs document-backend --lines 100
```

### 16.2 查看Nginx日志

```bash
# 访问日志
sudo tail -f /var/log/nginx/access.log

# 错误日志
sudo tail -f /var/log/nginx/error.log
```

### 16.3 重启服务

```bash
# 重启后端
pm2 restart document-backend

# 重启Nginx
sudo systemctl restart nginx
```

### 16.4 查看系统资源

```bash
# CPU和内存使用
htop

# 或使用
top
```

---

## 🐛 常见问题排查

### 问题1：无法访问网站

**检查清单：**

```bash
# 1. 检查Nginx是否运行
sudo systemctl status nginx

# 2. 检查后端是否运行
pm2 status

# 3. 检查端口是否监听
sudo netstat -tlnp | grep :80
sudo netstat -tlnp | grep :8000

# 4. 检查防火墙
sudo ufw status

# 5. 检查Nginx配置
sudo nginx -t
```

### 问题2：502 Bad Gateway

**原因：** 后端服务未启动或无法连接

**解决：**
```bash
# 检查后端状态
pm2 status

# 查看后端日志
pm2 logs document-backend

# 重启后端
pm2 restart document-backend
```

### 问题3：文件上传失败

**检查：**
```bash
# 1. 检查磁盘空间
df -h

# 2. 检查uploads目录权限
ls -la /var/www/document-tool/backend/uploads

# 3. 检查Nginx配置中的client_max_body_size
```

### 问题4：前端页面空白

**检查：**
```bash
# 1. 检查dist目录是否存在
ls -la /var/www/document-tool/frontend/dist

# 2. 检查Nginx配置中的root路径
# 3. 查看浏览器控制台错误
```

---

## 📝 快速命令参考

```bash
# 启动后端
pm2 start document-backend

# 停止后端
pm2 stop document-backend

# 重启后端
pm2 restart document-backend

# 查看后端状态
pm2 status

# 查看后端日志
pm2 logs document-backend

# 重启Nginx
sudo systemctl restart nginx

# 重载Nginx配置
sudo systemctl reload nginx

# 查看Nginx状态
sudo systemctl status nginx

# 测试Nginx配置
sudo nginx -t
```

---

## 🎉 部署完成检查清单

- [ ] 服务器环境已配置（Python, Node.js, Nginx）
- [ ] 项目文件已上传
- [ ] 前端已构建（dist目录存在）
- [ ] 后端依赖已安装
- [ ] Nginx配置已创建并启用
- [ ] 防火墙已配置
- [ ] 云服务器安全组已配置
- [ ] 后端服务已通过PM2启动
- [ ] 可以通过IP访问前端
- [ ] API接口可以正常访问
- [ ] 文件上传功能正常

---

## 📞 需要帮助？

如果遇到问题：

1. **查看日志**：`pm2 logs` 和 `sudo tail -f /var/log/nginx/error.log`
2. **检查服务状态**：`pm2 status` 和 `sudo systemctl status nginx`
3. **验证配置**：`sudo nginx -t`
4. **检查端口**：`sudo netstat -tlnp`

按照以上步骤，你应该能够成功部署项目。每一步都有详细的说明和预期输出，如果遇到问题，请参考"常见问题排查"部分。

