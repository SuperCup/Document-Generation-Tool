# 项目部署方案

## 方案一：内网穿透（最快，适合测试）

### 推荐工具：ngrok（免费，简单）

#### 步骤1：安装ngrok
1. 访问 https://ngrok.com/ 注册账号（免费）
2. 下载ngrok Windows版本
3. 解压到任意目录，如 `C:\ngrok`
4. 在ngrok网站获取你的authtoken
5. 运行命令配置token：
   ```bash
   ngrok config add-authtoken YOUR_TOKEN
   ```

#### 步骤2：修改后端CORS配置
运行 `配置外部访问.bat` 或手动修改 `backend/main.py` 中的CORS配置

#### 步骤3：启动服务
1. 启动后端：运行 `start_backend.bat`
2. 启动前端：运行 `start_frontend.bat`

#### 步骤4：启动ngrok
打开新的命令行窗口，运行：
```bash
# 同时暴露前端和后端
ngrok http 5173
```

或者分别暴露：
```bash
# 终端1：暴露后端
ngrok http 8000

# 终端2：暴露前端
ngrok http 5173
```

#### 步骤5：访问
ngrok会提供类似 `https://xxxx.ngrok.io` 的地址，通过这个地址访问。

**注意**：免费版ngrok每次启动URL会变化，付费版可以固定域名。

---

## 方案二：云服务器部署（正式，推荐）

### 推荐平台：阿里云/腾讯云/华为云

#### 步骤1：购买云服务器
- 配置建议：2核4G，带宽5M以上
- 系统：Ubuntu 20.04/22.04 或 Windows Server

#### 步骤2：配置服务器环境

**Ubuntu系统：**
```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装Python 3.10+
sudo apt install python3 python3-pip python3-venv -y

# 安装Node.js 18+
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# 安装Nginx（用于反向代理）
sudo apt install nginx -y
```

**Windows系统：**
- 安装Python 3.10+
- 安装Node.js 18+
- 安装IIS或使用Nginx for Windows

#### 步骤3：上传项目文件
使用FTP/SFTP或Git将项目上传到服务器

#### 步骤4：配置防火墙
```bash
# Ubuntu
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 8000/tcp
sudo ufw enable

# Windows：在防火墙设置中开放端口
```

#### 步骤5：配置Nginx反向代理

创建配置文件 `/etc/nginx/sites-available/document-tool`：

```nginx
server {
    listen 80;
    server_name your-domain.com;  # 替换为你的域名或IP

    # 前端静态文件
    location / {
        root /var/www/document-tool/frontend/dist;
        try_files $uri $uri/ /index.html;
        index index.html;
    }

    # 后端API代理
    location /api {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 文件上传大小限制
        client_max_body_size 100M;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        root /var/www/document-tool/frontend/dist;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

启用配置：
```bash
sudo ln -s /etc/nginx/sites-available/document-tool /etc/nginx/sites-enabled/
sudo nginx -t  # 测试配置
sudo systemctl reload nginx
```

#### 步骤6：使用PM2管理进程（推荐）

```bash
# 安装PM2
npm install -g pm2

# 启动后端
cd /path/to/project/backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
pm2 start "uvicorn main:app --host 0.0.0.0 --port 8000" --name backend

# 保存PM2配置
pm2 save
pm2 startup  # 设置开机自启
```

#### 步骤7：配置SSL证书（可选但推荐）

使用Let's Encrypt免费证书：
```bash
sudo apt install certbot python3-certbot-nginx -y
sudo certbot --nginx -d your-domain.com
```

---

## 方案三：Docker部署（容器化，推荐生产环境）

### 步骤1：创建Dockerfile

已创建 `Dockerfile` 和 `docker-compose.yml`，运行：
```bash
docker-compose up -d
```

### 步骤2：访问
通过服务器IP或域名访问

---

## 快速部署脚本

已创建以下脚本：
- `部署_内网穿透.bat` - Windows内网穿透部署
- `部署_云服务器.sh` - Linux云服务器部署
- `配置外部访问.bat` - 修改CORS配置

---

## 注意事项

1. **安全性**：
   - 生产环境必须使用HTTPS
   - 配置防火墙，只开放必要端口
   - 定期更新依赖包

2. **性能优化**：
   - 使用Nginx反向代理
   - 启用Gzip压缩
   - 配置静态资源缓存

3. **监控**：
   - 使用PM2监控进程
   - 配置日志轮转
   - 设置告警

4. **备份**：
   - 定期备份数据库（如有）
   - 备份上传的文件
   - 备份配置文件

---

## 常见问题

### Q: 外部无法访问？
A: 检查：
1. 防火墙是否开放端口
2. 云服务器安全组是否配置
3. CORS配置是否正确
4. 服务是否绑定到0.0.0.0而不是127.0.0.1

### Q: 上传大文件失败？
A: 检查：
1. Nginx的client_max_body_size配置
2. FastAPI的文件大小限制
3. 服务器磁盘空间

### Q: 性能慢？
A: 优化：
1. 使用Nginx反向代理
2. 启用Gzip压缩
3. 使用CDN加速静态资源
4. 考虑使用Redis缓存

