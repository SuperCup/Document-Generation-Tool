# 完整部署指南

本文档提供从零开始的完整部署流程，适用于云服务器部署。

---

## 📋 部署前准备

### 1. 准备云服务器

- **推荐配置**：
  - CPU: 2核
  - 内存: 4GB
  - 系统: Ubuntu 22.04 LTS
  - 磁盘: 40GB SSD
  - 带宽: 5Mbps

- **获取服务器信息**：
  - 公网IP地址
  - root密码或SSH密钥
  - SSH端口（默认22）

### 2. 准备本地环境

- Windows系统（当前环境）
- SSH客户端（Windows Terminal 或 PuTTY）
- FTP工具（可选，FileZilla 或 WinSCP）

---

## 🚀 部署步骤

### 步骤1：准备部署文件（Windows本地）

1. **运行部署准备脚本**：
   ```bash
   双击运行: 准备部署文件.bat
   ```

2. **脚本会自动完成**：
   - ✓ 检查项目结构
   - ✓ 安装前端依赖（如需要）
   - ✓ 构建前端（生成dist目录）
   - ✓ 创建部署包（deploy_package文件夹）

3. **等待完成**：
   - 脚本运行完成后，会在项目根目录生成 `deploy_package` 文件夹
   - 这个文件夹包含了所有需要上传到服务器的文件

### 步骤2：连接服务器

#### 方法1：使用Windows Terminal（推荐）

1. 打开Windows Terminal或PowerShell
2. 运行：
   ```bash
   ssh root@你的服务器IP
   ```
   例如：
   ```bash
   ssh root@123.456.789.012
   ```
3. 首次连接输入 `yes` 确认
4. 输入root密码

#### 方法2：使用PuTTY

1. 打开PuTTY
2. 输入服务器IP和端口22
3. 连接后输入root密码

### 步骤3：上传项目文件到服务器

#### 方法1：使用FTP工具（推荐，最简单）

1. **使用FileZilla**：
   - 打开FileZilla
   - 文件 -> 站点管理器 -> 新建站点
   - 协议：SFTP
   - 主机：你的服务器IP
   - 端口：22
   - 用户名：root
   - 密码：你的root密码
   - 连接
   - 左侧：选择本地的 `deploy_package` 文件夹
   - 右侧：进入 `/var/www`
   - 将整个 `deploy_package` 文件夹上传到 `/var/www`
   - 上传完成后，在服务器上重命名：
     ```bash
     mv /var/www/deploy_package /var/www/document-tool
     ```

#### 方法2：使用SCP命令

在Windows PowerShell中运行：
```powershell
scp -r deploy_package root@你的服务器IP:/var/www/document-tool
```

#### 方法3：使用Git（如果项目在Git仓库）

在服务器上运行：
```bash
cd /var/www
git clone <your-repo-url> document-tool
```

### 步骤4：运行一键部署脚本

1. **连接到服务器**（如果已断开）

2. **进入项目目录**：
   ```bash
   cd /var/www/document-tool
   ```

3. **给脚本添加执行权限**：
   ```bash
   chmod +x 一键部署脚本.sh
   ```

4. **运行部署脚本**：
   ```bash
   sudo bash 一键部署脚本.sh
   ```

5. **按照提示操作**：
   - 脚本会自动安装所有依赖
   - 构建前端
   - 配置后端
   - 配置Nginx
   - 启动服务
   - 配置防火墙

6. **输入域名**（可选）：
   - 如果有域名，输入域名
   - 如果没有域名，直接回车使用IP访问

### 步骤5：配置云服务器安全组

**重要**：必须在云服务器控制台配置安全组，否则无法从外部访问！

#### 阿里云
1. 登录阿里云控制台
2. 进入 **云服务器ECS** -> **实例**
3. 点击你的服务器实例
4. 点击 **安全组** -> **配置规则**
5. 添加规则：
   - **端口范围**: `80/80`
   - **授权对象**: `0.0.0.0/0`
   - **协议类型**: `TCP`
   - **描述**: HTTP
6. 同样添加 `443` 端口（HTTPS）

#### 腾讯云
1. 登录腾讯云控制台
2. 进入 **云服务器** -> **实例**
3. 点击你的服务器 -> **安全组**
4. 点击 **修改规则** -> **添加规则**
5. 配置：
   - **类型**: `自定义`
   - **协议端口**: `TCP:80`
   - **来源**: `0.0.0.0/0`
6. 同样添加 `TCP:443`

#### 华为云
1. 登录华为云控制台
2. 进入 **弹性云服务器** -> **安全组**
3. 选择你的安全组 -> **入方向规则** -> **添加规则**
4. 配置：
   - **协议端口**: `TCP:80`
   - **源地址**: `0.0.0.0/0`
5. 同样添加 `TCP:443`

### 步骤6：验证部署

#### 方法1：使用验证脚本（推荐）

在服务器上运行：
```bash
cd /var/www/document-tool
chmod +x 部署验证脚本.sh
bash 部署验证脚本.sh
```

脚本会自动检查所有配置和服务状态。

#### 方法2：手动验证

1. **检查服务状态**：
   ```bash
   # 检查后端
   pm2 status
   
   # 检查Nginx
   sudo systemctl status nginx
   ```

2. **测试API**：
   ```bash
   curl http://localhost:8000/
   ```
   应该返回：`{"message":"文档生成工具 API"}`

3. **浏览器访问**：
   - 打开浏览器
   - 访问：`http://你的服务器IP`
   - 应该能看到应用界面

---

## ✅ 部署完成检查清单

使用 `部署检查清单.md` 逐项检查：

- [ ] 服务器环境已安装（Python, Node.js, Nginx, PM2）
- [ ] 项目文件已上传到 `/var/www/document-tool`
- [ ] 前端已构建（dist目录存在）
- [ ] 后端依赖已安装
- [ ] Nginx配置已创建并启用
- [ ] 防火墙已配置
- [ ] 云服务器安全组已配置
- [ ] 后端服务已通过PM2启动
- [ ] 可以通过IP访问前端
- [ ] API接口可以正常访问

---

## 🔧 常见问题

### Q1: 无法连接服务器？

**检查**：
- 服务器是否运行
- IP地址是否正确
- 安全组是否开放22端口
- 密码是否正确

### Q2: 部署脚本运行失败？

**检查**：
- 是否使用 `sudo` 运行
- 网络连接是否正常
- 服务器磁盘空间是否充足
- 查看错误信息，根据提示修复

### Q3: 无法访问网站？

**检查清单**：
```bash
# 1. 检查Nginx
sudo systemctl status nginx

# 2. 检查后端
pm2 status

# 3. 检查端口
sudo netstat -tlnp | grep :80
sudo netstat -tlnp | grep :8000

# 4. 检查防火墙
sudo ufw status

# 5. 检查Nginx配置
sudo nginx -t
```

### Q4: 502 Bad Gateway？

**原因**：后端服务未启动或无法连接

**解决**：
```bash
# 检查后端状态
pm2 status

# 查看后端日志
pm2 logs document-backend

# 重启后端
pm2 restart document-backend
```

### Q5: 前端页面空白？

**检查**：
```bash
# 1. 检查dist目录
ls -la /var/www/document-tool/frontend/dist

# 2. 检查Nginx配置中的root路径
sudo cat /etc/nginx/sites-available/document-tool

# 3. 查看浏览器控制台错误
```

---

## 📊 部署后维护

### 查看日志

```bash
# 后端日志
pm2 logs document-backend

# Nginx访问日志
sudo tail -f /var/log/nginx/access.log

# Nginx错误日志
sudo tail -f /var/log/nginx/error.log
```

### 重启服务

```bash
# 重启后端
pm2 restart document-backend

# 重启Nginx
sudo systemctl restart nginx

# 重启所有服务
pm2 restart all
sudo systemctl restart nginx
```

### 更新代码

```bash
# 1. 上传新代码到服务器
# 2. 重新构建前端（如需要）
cd /var/www/document-tool/frontend
npm run build

# 3. 重启后端
pm2 restart document-backend

# 4. 重载Nginx
sudo systemctl reload nginx
```

---

## 🔒 安全建议

1. **配置HTTPS**（生产环境必须）：
   ```bash
   sudo apt install certbot python3-certbot-nginx
   sudo certbot --nginx -d your-domain.com
   ```

2. **限制CORS来源**：
   在服务器上设置环境变量：
   ```bash
   export ALLOWED_ORIGINS=http://your-domain.com,https://your-domain.com
   ```

3. **定期更新**：
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

4. **配置防火墙规则**：
   只开放必要的端口

---

## 📞 需要帮助？

如果遇到问题：

1. **查看详细文档**：
   - `云服务器部署详细指南.md` - 详细步骤说明
   - `部署检查清单.md` - 检查清单
   - `故障排除.md` - 故障排除指南

2. **检查日志**：
   - `pm2 logs document-backend` - 后端日志
   - `sudo tail -f /var/log/nginx/error.log` - Nginx错误日志

3. **运行验证脚本**：
   ```bash
   bash 部署验证脚本.sh
   ```

---

## 🎉 部署成功！

部署完成后，你可以：

- 访问：`http://你的服务器IP`
- 上传PPT和Excel文件
- 插入图片并生成文档
- 下载处理后的文件

祝你使用愉快！

