# 阿里云ECS配置建议

## 🎯 预装应用选择建议

### ❌ **不需要选择预装应用**（推荐）

**原因：**
1. 我们的部署脚本会自动安装所有需要的环境（Python、Node.js、Nginx等）
2. 预装应用可能会占用资源或产生冲突
3. 从零开始安装更干净，便于管理

**推荐配置：**
- ✅ **操作系统**：选择 `Ubuntu 22.04 64位`（已选中）
- ❌ **预装应用**：不选择任何预装应用
- ✅ **地域**：选择离你最近的地域（如：华南3(广州)）

---

## 📋 完整配置清单

### 操作系统
- ✅ **Ubuntu 22.04 64位**（推荐）
  - 原因：稳定、兼容性好、社区支持完善

### 预装应用
- ❌ **不选择任何预装应用**
  - 原因：我们的脚本会安装所需环境，避免冲突

### 实例规格
- **4核(vCPU) 8GiB**（免费试用规格）
  - 足够运行我们的应用
  - 如果后续需要，可以升级

### 云盘
- **ESSD Entry 40GiB**（默认）
  - 足够存储项目文件和上传的文件

### 地域选择
- 选择离你最近的地域
- 如果主要用户在国内，选择中国内地地域
- 如果主要用户在国外，选择非中国内地地域（有更多免费流量）

---

## 🚀 购买后的操作流程

### 1. 获取服务器信息
购买后，在阿里云控制台：
- 找到你的ECS实例
- 记录 **公网IP地址**
- 设置/重置 **root密码**

### 2. 配置安全组（重要！）

在ECS控制台：
1. 点击你的实例
2. 进入 **安全组** -> **配置规则**
3. 添加以下规则：

| 规则方向 | 授权策略 | 协议类型 | 端口范围 | 授权对象 |
|---------|---------|---------|---------|---------|
| 入方向 | 允许 | TCP | 22/22 | 0.0.0.0/0 |
| 入方向 | 允许 | TCP | 80/80 | 0.0.0.0/0 |
| 入方向 | 允许 | TCP | 443/443 | 0.0.0.0/0 |

### 3. 连接服务器

```bash
ssh root@你的公网IP
```

### 4. 开始部署

**方法一：使用一键脚本（推荐）**
```bash
# 上传一键部署脚本.sh到服务器
sudo bash 一键部署脚本.sh
```

**方法二：手动部署**
按照 `云服务器部署详细指南.md` 逐步操作

---

## 💡 如果选择了预装应用怎么办？

### 如果选择了Docker
可以使用我们的Docker部署方案：
```bash
docker-compose up -d
```

### 如果选择了宝塔面板
1. 通过宝塔面板安装Python、Node.js、Nginx
2. 然后按照我们的部署指南继续操作
3. 注意：宝塔面板会占用8888端口

### 如果选择了LNMP
LNMP包含Nginx，需要：
1. 检查Nginx是否已安装：`nginx -v`
2. 如果已安装，跳过Nginx安装步骤
3. 直接配置Nginx反向代理

---

## ⚠️ 重要提示

1. **安全组必须配置**：否则无法从外部访问
2. **root密码要记好**：首次登录需要
3. **免费额度有限**：注意使用时长和流量
4. **建议选择Ubuntu**：我们的脚本主要针对Ubuntu优化

---

## 📝 推荐配置总结

```
操作系统：Ubuntu 22.04 64位 ✅
预装应用：无 ❌
实例规格：4核8GiB（试用规格）
云盘：40GiB ESSD Entry
地域：选择离你最近的地域
安全组：开放22, 80, 443端口
```

按照这个配置购买后，直接使用我们的部署脚本即可！

