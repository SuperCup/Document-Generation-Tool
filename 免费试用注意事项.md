# 阿里云ECS免费试用 - 避免扣费指南

## ⚠️ 重要：避免意外扣费

### 1. 免费额度说明

根据你的试用信息：
- ✅ **ECS实例费用**：免费额度 300元，剩余 300.000元
- ✅ **每小时免费额度**：0.833元，剩余 0.579元
- ✅ **公网流量（中国内地）**：每月免费 20GB，已使用 0.000 GB
- ✅ **公网流量（非中国内地）**：每月免费 200GB，已使用 0.000 GB

### 2. 可能产生费用的场景

#### ⚠️ 会产生费用的操作：

1. **超出免费流量**
   - 中国内地：超过 20GB/月
   - 非中国内地：超过 200GB/月
   - **计费标准**：约 0.8元/GB

2. **超出免费时长**
   - 免费额度用完后继续使用
   - **计费标准**：按小时计费

3. **升级配置**
   - 增加CPU、内存、磁盘
   - **会产生额外费用**

4. **购买额外资源**
   - 额外云盘、快照等
   - **会产生费用**

#### ✅ 不会产生费用的操作：

- 使用免费额度内的实例
- 使用免费额度内的流量
- 在免费时长内使用

### 3. 避免扣费的措施

#### 措施1：设置自动释放（推荐）

```bash
# 在阿里云控制台设置
# 实例 -> 更多 -> 释放设置 -> 设置自动释放时间
```

**建议设置：**
- 如果只是测试：设置3天后自动释放
- 如果正式使用：不设置自动释放，但设置余额提醒

#### 措施2：设置余额提醒

1. 登录阿里云控制台
2. 进入 **费用中心** -> **费用预警**
3. 设置余额提醒：
   - 余额低于 50元 时提醒
   - 余额低于 10元 时提醒

#### 措施3：监控流量使用

定期检查流量使用情况：
- 在ECS控制台查看 **监控** 标签页
- 关注 **网络流量** 指标
- 如果接近免费额度，及时停止服务

#### 措施4：设置费用预算

1. 进入 **费用中心** -> **预算管理**
2. 创建预算：
   - 预算金额：10元（作为安全缓冲）
   - 提醒阈值：80%
   - 超出时：发送通知

#### 措施5：使用内网访问（如果可能）

- 如果前端和后端在同一服务器，使用 `localhost` 或 `127.0.0.1`
- 减少公网流量消耗

### 4. 当前实例状态检查

你的实例信息：
- **实例ID**: i-7xvg439r0716r4k66ut8
- **公网IP**: 8.134.59.82
- **状态**: 运行中
- **系统**: Ubuntu 22.04 64位
- **配置**: 2核4GiB
- **支付方式**: 按量付费

**当前安全状态：**
- ✅ 免费额度充足
- ✅ 流量使用为0
- ⚠️ 需要配置安全组才能访问

---

## 🛡️ 安全建议

### 1. 立即配置安全组（必须！）

**步骤：**

1. 在ECS控制台，点击你的实例
2. 点击 **网络与安全组** 标签
3. 点击 **安全组ID**（蓝色链接）
4. 点击 **配置规则**
5. 点击 **添加安全组规则**

**添加以下规则：**

| 规则方向 | 授权策略 | 协议类型 | 端口范围 | 授权对象 | 描述 |
|---------|---------|---------|---------|---------|------|
| 入方向 | 允许 | TCP | 22/22 | 0.0.0.0/0 | SSH连接 |
| 入方向 | 允许 | TCP | 80/80 | 0.0.0.0/0 | HTTP访问 |
| 入方向 | 允许 | TCP | 443/443 | 0.0.0.0/0 | HTTPS访问 |

**⚠️ 注意：**
- 端口22可以限制为你的IP（更安全）
- 端口80和443需要开放给所有IP（用于Web访问）

### 2. 设置实例密码

1. 在实例页面，点击 **重置密码**
2. 设置一个强密码（至少8位，包含大小写字母、数字、特殊字符）
3. 重启实例使密码生效

---

## 📊 费用监控

### 查看实时费用

1. 进入 **费用中心** -> **费用账单**
2. 查看 **实时账单**
3. 关注 **ECS** 和 **CDT（流量）** 费用

### 设置费用预警

1. **费用中心** -> **费用预警**
2. 设置：
   - 余额低于 50元：发送短信/邮件
   - 余额低于 10元：发送短信/邮件
   - 日消费超过 5元：发送通知

---

## 🎯 下一步操作

### 立即执行（按顺序）

#### 步骤1：配置安全组（5分钟）

1. 在ECS控制台，点击实例ID
2. 点击 **网络与安全组** 标签
3. 点击安全组ID -> **配置规则** -> **添加安全组规则**
4. 添加端口 22, 80, 443 的入站规则

#### 步骤2：重置密码（2分钟）

1. 在实例页面，点击 **重置密码**
2. 设置新密码并记录
3. 点击 **重启** 使密码生效

#### 步骤3：连接服务器（3分钟）

在本地电脑打开命令行（Windows Terminal或PowerShell）：

```bash
ssh root@8.134.59.82
```

输入刚才设置的密码。

#### 步骤4：上传项目文件（10分钟）

**方法1：使用Git（如果有Git仓库）**
```bash
# 在服务器上
cd /var/www
git clone <your-repo-url> document-tool
```

**方法2：使用FTP工具（FileZilla）**
1. 下载FileZilla
2. 连接服务器（SFTP，端口22）
3. 上传整个项目文件夹到 `/var/www/document-tool`

**方法3：使用scp命令（在本地项目目录）**
```bash
scp -r . root@8.134.59.82:/var/www/document-tool/
```

#### 步骤5：运行部署脚本（15分钟）

```bash
# 在服务器上
cd /var/www/document-tool
sudo bash 一键部署脚本.sh
```

脚本会自动完成所有配置。

#### 步骤6：测试访问（2分钟）

在浏览器访问：
```
http://8.134.59.82
```

---

## 💰 费用预估

### 正常使用情况（免费额度内）

- **实例费用**：0元（免费额度内）
- **流量费用**：0元（20GB免费流量足够）
- **总计**：**0元**

### 如果超出免费额度

- **实例费用**：约 0.8元/小时（2核4GiB）
- **流量费用**：约 0.8元/GB（超出部分）
- **建议**：设置余额提醒，避免意外消费

---

## 🔔 重要提醒

1. **定期检查费用**：每天查看一次费用账单
2. **设置余额提醒**：避免余额不足导致服务中断
3. **监控流量使用**：在ECS控制台的监控页面查看
4. **及时释放资源**：测试完成后及时释放实例
5. **不要升级配置**：升级会产生额外费用

---

## ✅ 检查清单

部署前确认：
- [ ] 安全组已配置（端口22, 80, 443已开放）
- [ ] 实例密码已设置
- [ ] 可以通过SSH连接服务器
- [ ] 项目文件已上传到服务器
- [ ] 余额提醒已设置

部署后确认：
- [ ] 可以通过IP访问网站
- [ ] 文件上传功能正常
- [ ] 费用账单正常（0元或接近0元）
- [ ] 流量使用在免费额度内

---

按照以上步骤操作，可以安全使用免费试用，避免意外扣费！

