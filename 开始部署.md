# 🚀 快速开始部署

## 3步完成部署

### 第1步：准备部署文件（Windows本地）

双击运行：**`准备部署文件.bat`**

这个脚本会：
- ✓ 自动检查项目结构
- ✓ 安装前端依赖（如需要）
- ✓ 构建前端
- ✓ 创建部署包（`deploy_package` 文件夹）

**等待脚本完成**（约2-5分钟）

---

### 第2步：上传到服务器

#### 方法1：使用FileZilla（推荐，最简单）

1. 下载并安装 [FileZilla](https://filezilla-project.org/)
2. 打开FileZilla -> 文件 -> 站点管理器
3. 新建站点：
   - 协议：**SFTP**
   - 主机：**你的服务器IP**
   - 端口：**22**
   - 用户名：**root**
   - 密码：**你的root密码**
4. 连接
5. 左侧：选择本地的 `deploy_package` 文件夹
6. 右侧：进入 `/var/www`
7. 将整个 `deploy_package` 文件夹拖到右侧上传
8. 上传完成后，在服务器SSH中运行：
   ```bash
   mv /var/www/deploy_package /var/www/document-tool
   ```

#### 方法2：使用SCP命令

在Windows PowerShell中运行：
```powershell
scp -r deploy_package root@你的服务器IP:/var/www/document-tool
```

---

### 第3步：运行部署脚本（服务器上）

1. **连接服务器**：
   ```bash
   ssh root@你的服务器IP
   ```

2. **运行部署脚本**：
   ```bash
   cd /var/www/document-tool
   chmod +x 一键部署脚本.sh
   sudo bash 一键部署脚本.sh
   ```

3. **等待完成**（约10-15分钟）：
   - 脚本会自动安装所有环境
   - 构建前端
   - 配置后端
   - 启动服务

4. **配置安全组**（重要！）：
   - 登录云服务器控制台
   - 在安全组中开放 **80** 和 **443** 端口
   - 授权对象：`0.0.0.0/0`

5. **访问**：
   - 浏览器打开：`http://你的服务器IP`
   - 完成！🎉

---

## 📋 详细文档

- **完整部署指南**：`完整部署指南.md` - 详细的步骤说明
- **部署检查清单**：`部署检查清单.md` - 逐项检查清单
- **云服务器部署详细指南**：`云服务器部署详细指南.md` - 非常详细的说明

---

## ❓ 遇到问题？

1. **查看日志**：
   ```bash
   pm2 logs document-backend
   sudo tail -f /var/log/nginx/error.log
   ```

2. **运行验证脚本**：
   ```bash
   bash 部署验证脚本.sh
   ```

3. **查看故障排除**：`故障排除.md`

---

## ✅ 部署成功标志

- ✓ 可以通过 `http://你的服务器IP` 访问前端
- ✓ 可以通过 `http://你的服务器IP/api/` 访问后端API
- ✓ 可以上传文件
- ✓ 可以下载生成的文件

---

**祝你部署顺利！** 🎉

