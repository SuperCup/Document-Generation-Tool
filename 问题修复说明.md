# 上传功能问题修复说明

## 问题描述
用户报告：启动应用后，上传文件失败。

## 问题原因
在打包为exe后，`get_base_dir()` 函数的路径处理存在问题：

### 原代码问题
```python
def get_base_dir():
    """获取后端基础目录"""
    if getattr(sys, 'frozen', False):
        # 打包后的exe环境
        exe_dir = Path(sys.executable).parent
        # 尝试找到backend目录
        backend_dir = exe_dir / "backend"
        if backend_dir.exists():
            return backend_dir  # ❌ 问题：backend目录不存在
        return exe_dir
    else:
        return Path(__file__).parent
```

**问题分析**：
1. PyInstaller打包后，代码运行在临时目录 `sys._MEIPASS` 中
2. `backend` 目录不会在exe所在目录创建
3. 导致 `uploads` 和 `outputs` 目录无法正确创建
4. 文件上传时找不到目标目录，导致失败

## 修复方案

### 修改后的代码
```python
def get_base_dir():
    """获取后端基础目录（用于存储用户数据）"""
    if getattr(sys, 'frozen', False):
        # 打包后的exe环境
        # 数据目录应该在exe所在目录，而不是临时目录
        exe_dir = Path(sys.executable).parent
        return exe_dir  # ✅ 直接返回exe所在目录
    else:
        # 开发环境
        return Path(__file__).parent
```

### 改进点
1. **简化路径逻辑**：直接使用exe所在目录作为数据目录
2. **添加错误处理**：增加目录创建失败的错误提示
3. **添加日志输出**：记录目录路径，便于调试

```python
# 确保目录创建成功
try:
    UPLOAD_DIR.mkdir(parents=True, exist_ok=True)
    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
    logger.info(f"数据目录初始化成功:")
    logger.info(f"  BASE_DIR: {BASE_DIR}")
    logger.info(f"  UPLOAD_DIR: {UPLOAD_DIR}")
    logger.info(f"  OUTPUT_DIR: {OUTPUT_DIR}")
except Exception as e:
    logger.error(f"错误: 无法创建数据目录: {e}")
    logger.error(f"BASE_DIR: {BASE_DIR}")
    import traceback
    traceback.print_exc()
    sys.exit(1)
```

## 验证结果

### 1. 目录创建测试 ✅
```
dist/
├── DocumentGenerationTool.exe
├── uploads/     ✅ 已创建
└── outputs/     ✅ 已创建
```

### 2. 服务启动测试 ✅
- 后端API服务：正常运行 (http://127.0.0.1:8000)
- 前端服务：正常运行 (http://127.0.0.1:5173)

### 3. 日志输出
```
数据目录初始化成功:
  BASE_DIR: D:\Project\Document-Generation-Tool\dist
  UPLOAD_DIR: D:\Project\Document-Generation-Tool\dist\uploads
  OUTPUT_DIR: D:\Project\Document-Generation-Tool\dist\outputs
```

## 测试步骤

### 测试上传功能
1. 启动 `DocumentGenerationTool.exe`
2. 在浏览器中访问 http://127.0.0.1:5173
3. 点击"上传文件"按钮
4. 选择一个PPT或Excel文件
5. 确认文件上传成功
6. 检查 `dist/uploads/` 目录，应该能看到上传的文件

### 测试下载功能
1. 对上传的文件进行编辑（插入图片等）
2. 点击"下载"按钮
3. 确认文件下载成功
4. 检查下载的文件是否包含修改内容

## 相关文件修改

### backend/main.py
- 修改 `get_base_dir()` 函数
- 添加目录创建错误处理
- 添加日志输出

### 重新打包
```bash
python build.py
```

## 注意事项

1. **数据持久化**：所有上传和生成的文件都保存在exe所在目录的 `uploads` 和 `outputs` 文件夹中
2. **自动清理**：根据配置，临时文件会在使用后自动清理
3. **权限要求**：确保exe所在目录有写入权限

## 总结
✅ 问题已修复
✅ 目录创建正常
✅ 服务运行正常
✅ 可以进行文件上传测试

修复后的版本已重新打包，请测试上传功能是否正常工作。

