# 部署检查清单

使用此清单确保每一步都正确完成。

## ✅ 服务器准备

- [ ] 已购买云服务器
- [ ] 已获取服务器IP地址
- [ ] 已获取root密码或SSH密钥
- [ ] 可以使用SSH连接到服务器

## ✅ 环境安装

- [ ] Python 3.10+ 已安装
- [ ] pip 已安装
- [ ] Node.js 18+ 已安装
- [ ] npm 已安装
- [ ] Nginx 已安装并运行
- [ ] PM2 已安装

**验证命令：**
```bash
python3 --version  # 应显示 3.10+
node --version     # 应显示 v18+
npm --version      # 应显示 9+
nginx -v           # 应显示版本号
pm2 --version      # 应显示版本号
```

## ✅ 项目部署

- [ ] 项目文件已上传到 `/var/www/document-tool`
- [ ] 文件权限已正确设置
- [ ] 前端依赖已安装（`npm install` 成功）
- [ ] 前端已构建（`npm run build` 成功，dist目录存在）
- [ ] 后端虚拟环境已创建
- [ ] 后端依赖已安装（`pip install -r requirements.txt` 成功）
- [ ] uploads 和 outputs 目录已创建

**验证命令：**
```bash
ls -la /var/www/document-tool/frontend/dist  # 应看到index.html
ls -la /var/www/document-tool/backend/venv    # 应看到虚拟环境
```

## ✅ 服务配置

- [ ] Nginx配置文件已创建：`/etc/nginx/sites-available/document-tool`
- [ ] Nginx配置已启用（符号链接已创建）
- [ ] Nginx配置测试通过（`sudo nginx -t` 成功）
- [ ] 后端服务已通过PM2启动
- [ ] PM2配置已保存（`pm2 save` 已执行）
- [ ] PM2开机自启已配置

**验证命令：**
```bash
sudo nginx -t                    # 应显示 "test is successful"
pm2 status                       # 应显示 document-backend 为 online
pm2 list                         # 应看到后端服务
```

## ✅ 网络配置

- [ ] 防火墙已配置（端口80, 443, 8000已开放）
- [ ] 云服务器安全组已配置（端口80, 443已开放）
- [ ] 可以通过 `http://服务器IP` 访问前端
- [ ] 可以通过 `http://服务器IP/api/` 访问后端API

**验证命令：**
```bash
sudo ufw status                  # 应显示 80, 443, 8000 已允许
curl http://localhost:8000/api/  # 应返回 {"message":"文档生成工具 API"}
```

## ✅ 功能测试

- [ ] 前端页面可以正常打开
- [ ] 可以上传PPT文件
- [ ] 可以上传Excel文件
- [ ] 可以上传图片
- [ ] 可以下载生成的文件
- [ ] 远程文件浏览器功能正常

## ✅ 安全配置（可选）

- [ ] SSL证书已配置（HTTPS）
- [ ] CORS已限制为特定域名（生产环境）
- [ ] 防火墙规则已优化
- [ ] 定期备份已配置

## 🎯 完成标志

当所有项目都打勾后，部署就完成了！

访问地址：`http://你的服务器IP`

